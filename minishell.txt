Parser
    parser
    quotes
    environment
    token
    variables
Parse Phases
    split_commands
    tokenization
    expand_token
    separate_tokens
    create_command
    create_input
    redirections
    expansor
    heredoc


El exec es ruta del comando original(puntero) los argumentos con el comando(puntero doble) y el env (puntero doble)